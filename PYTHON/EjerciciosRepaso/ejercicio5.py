# Ejercicio 5

# Utiliza la función "range" e imprime por pantalla cada número del -10 al -1. 
# En ese orden. Consulta la documentación de range

funcionRango = range(-10, 0)
for n in funcionRango:
    print(n)

#EXPLAINED:
    # if funcionRango is the range function that goes from -10 to 0, for n (which is every element in the range) 
        #in the function x (the range) print every number

# Note: range prints the first number but not the last one, so if like in this one we want to print til -1,
        # we need to state 0 in the range 
