# Concatena todos los elementos de la tupla en un único string. Para ello utiliza el metodo .join() de los Strings

# ejer_25 = ("cien", "cañones", "por", "banda")

# Resultado: cien cañones por banda

ejer_25 = ("cien", "cañones", "por", "banda")

fraseCompleta = " ".join(ejer_25)
print(fraseCompleta)
